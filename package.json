{
  "name": "messenger",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "npm": ">=9.0.0",
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp src/server/server.js build",
    "preview": "vite preview",
    "start": "npm run build && node build/server.js"
  },
  "devDependencies": {
    "sass": "^1.62.1",
    "vite": "^4.3.9",
    "vite-plugin-handlebars": "^1.6.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "handlebars": "^4.7.7"
  }
}

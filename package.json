{
  "name": "messenger",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines" : {
    "npm" : ">=9.0.0",
    "node" : ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp src/server/server.js build && cp package.json build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "devDependencies": {
    "sass": "^1.62.1",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
